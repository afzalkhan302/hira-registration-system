# Copy to client/.env for local dev (optional) or set in Vercel for production.
#
# Base URL of the backend API.
#  - Local dev: leave EMPTY — Vite proxies /api to http://localhost:5000.
#  - Production: the deployed backend origin, e.g. https://hira-api.onrender.com
VITE_API_URL=
